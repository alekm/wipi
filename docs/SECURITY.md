# Security

## Sensitive Data Review

### Addressed

- **resident-sim.yaml** - Was tracked with real DPSK passphrases. Removed from git; now ignored via `controller/scenarios/*.yaml`. This file is auto-generated by the resident simulator.

### Already Protected (.gitignore)

- `.env` - Credentials (AGENT_API_KEY, WIPI_ADMIN_PASSWORD_HASH, etc.)
- `data/*.db` - SQLite database (may contain PSKs, Ruckus One credentials)
- `controller_config.yaml`, `agent_config.yaml` - Local config overrides

### Default Credentials (Change Before Production)

For demo/convenience, these defaults exist in code and docker-compose:

- **AGENT_API_KEY**: `387d5f76...` (64-char hex)
- **Admin password**: `Ruckus123!` (hash: `8f4179b458b4e...`)

Run `./scripts/wipi-init` to generate new credentials. See README and CLAUDE.md.

## If resident-sim.yaml Was Pushed

If the repo was already pushed to GitHub with `resident-sim.yaml` containing real DPSKs:

1. **Rotate the passphrases** in your Wi-Fi controller (RUCKUS One / MDU 360). The exposed passphrases are compromised.
2. **Rewrite git history** to remove the file (optional; use `git filter-repo` or BFG Repo-Cleaner).
